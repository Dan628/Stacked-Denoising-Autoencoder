from setuptools import setup, find_packages

setup(name='SDAE',
      version ='0.1',
      description = 'Stacked denoising autoencoder for feature extraction.',
      packages = find_packages(),
      zip_safe=False,
      )

